services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: flop_club
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "55432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
