{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run prisma:generate:prod --workspace @flop-club/api && npm run build --workspace @flop-club/api"
  },
  "deploy": {
    "startCommand": "npm run prisma:deploy:prod --workspace @flop-club/api && npm run start --workspace @flop-club/api",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
